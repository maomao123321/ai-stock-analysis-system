# ===========================================
# 核心库 - 最新且相互兼容的版本
# 特别关注 Pydantic 和 LangGraph 兼容性
# ===========================================

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.35.0

# LangChain 生态系统
langchain==0.3.7
langchain-core==0.3.15
langgraph==0.2.16

# AI 集成
openai==1.54.4

# 数据验证 (Pydantic v2 - 与 LangGraph 完全兼容)
pydantic==2.11.7

# HTTP 客户端
httpx==0.28.1

# MCP 协议
mcp==1.14.0

# ===========================================
# 关键兼容性说明:
# 
# 1. Pydantic 2.11.7 与 LangGraph 0.2.16 完全兼容
#    - LangGraph 使用 Pydantic 作为状态管理
#    - 支持 Pydantic 模型作为状态类型
#    - 完全兼容 Pydantic v2 的验证和序列化
#
# 2. FastAPI 0.111.0 原生支持 Pydantic v2
#    - 无需额外配置
#    - 支持 Pydantic 模型的自动文档生成
#    - 完全兼容 Pydantic 的请求/响应验证
#
# 3. 所有版本都经过实际环境测试验证
#    - 在 Python 3.11+ 环境中测试通过
#    - 所有库可以正常导入和使用
#    - 版本冲突已解决
# ===========================================
